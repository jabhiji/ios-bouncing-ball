ios-bouncing-ball
=================

A simple iOS animation using NSTimer and the Model-View-Contoller paradigm

This code simulates a pair of balls bouncing around inside a 2D rectangular box. The user can start and stop the animation using the buttons below the box.
